/**
 * This is unit interface
 * @author Ratikanta
 * @since 1.0.0
 * @interface IUnit
 */
interface IUnit{
    id: number,
    unitName: string,
    slugidunit: number,
    createdDate:string,
    lastModified:string
}