/**
 * This is subgroup interface
 * @author Ratikanta
 * @since 1.0.0
 * @interface ISubgroup
 */
interface ISubgroup{
    id: number,
    subgroupName: string,
    slugidsubgroup: number,
    createdDate:string,
    lastModified:string
}