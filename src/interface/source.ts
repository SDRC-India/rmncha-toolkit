/**
 * This is source interface
 * @author Ratikanta
 * @since 1.0.0
 * @interface ISource
 */
interface ISource{
    id: number,
    sourceName: string,
    slugidsource: number,
    createdDate:string,
    lastModified:string
}