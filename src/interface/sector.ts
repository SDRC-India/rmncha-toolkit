/**
 * This is area interface
 * @author Ratikanta
 * @since 1.0.0
 * @interface ISector
 */
interface ISector{
    id: number,
    sectorName: string,
    slugidsector: number,
    createdDate:string,
    lastModified:string
}